# Optional audio dependencies — install to enable STT/TTS voice I/O.
# Core runtime (requirements.txt) stays stdlib-only without these.
#
#   pip install -r requirements-audio.txt
#
# macOS: `say` is built-in; no TTS package needed for the say fallback.
# faster-whisper downloads the STT model (~1.6 GB) on first use.

numpy>=1.24
scipy>=1.10
faster-whisper>=1.0       # STT: ATC-fine-tuned Whisper via CTranslate2
piper-tts>=1.1            # TTS: local ONNX voice synthesis (optional; say fallback works without it)
sounddevice>=0.4          # CLI PTT recording only (not needed for Tauri UI path)
