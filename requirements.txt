# Backend runtime — the default ElevenLabs + Claude path needs only these.
#   pip install -r requirements.txt
#
# Claude is reached through the `claude` CLI (no Python package / key here).
# Offline STT/TTS and CLI mic capture live in requirements-local.txt.
# The terminal CLI (main.py) and the xplane/airport/atc modules stay stdlib-only.
websockets>=12.0
numpy>=1.24
scipy>=1.10
